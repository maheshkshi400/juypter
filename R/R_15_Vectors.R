# atomic vector of the type character
print("abc");

# atomic vector of the type double
print(12.5);

# atomic vector of the type integer
print(63L);

# atomic vector of the type logical
print("abc");

# atomic vector of the type complex
print(2+3i);

# atomic vector of the type raw
print(charToRaw('hello'));


# modifying vectors

# Creating two vectors

v1 <- c(3,8,4,5,0,11)

v2 <- c(4,11,0,8.1,2)

# addtion of vectors

add.result <- v1+v2
print(add.result)

substract.result <- v1-v2
print(substract.result)

mul.result <- v1*v2
print(mul.result)

div.result <- v1/v2
print(div.result)

